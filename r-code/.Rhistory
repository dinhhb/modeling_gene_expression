anno_df <- data.frame(batch = se_object$batch)
rownames(anno_df) <- colnames(top_mat)
# Plot
pheatmap(
top_mat,
annotation_col = anno_df,
show_rownames  = TRUE,
show_colnames  = TRUE,
fontsize_row   = 7,
fontsize_col   = 4,
angle_col      = 45,
scale          = "row",
cluster_rows   = TRUE,
cluster_cols   = TRUE,
main           = "Top 38 Variable Genes by Batch"
)
clear
cls
# Install CRAN packages
install.packages(c("data.table", "ggplot2", "ggprism"), dependencies = TRUE)
# Install Bioconductor packages
if (!require("BiocManager", quietly = TRUE))
install.packages("BiocManager")
BiocManager::install(c("GEOquery", "limma", "edgeR", "sva", "lme4"), dependencies = TRUE)
# Load data
exprmx <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/gene_expressions_matrix_clean.csv")
meta <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/sample_source.csv")
View(exprmx)
# Load data
exprmx <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/gene_expressions_matrix_clean.csv")
meta <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/sample_source.csv")
View(exprmx)
View(meta)
View(exprmx)
meta$batch <- as.factor(meta$source)
# Filter low-expressed genes
perc_keep <- 0.8    # keep genes expressed at least 80% of samples
gene_keep <- rowSums(exprmx > 0) >= ceiling(perc_keep * ncol(exprmx))
count_tbl_low_rm <- as.matrix(exprmx[gene_keep, ])
View(count_tbl_low_rm)
# Install CRAN packages
install.packages(c("data.table", "ggplot2", "ggprism"), dependencies = TRUE)
# Install Bioconductor packages
if (!require("BiocManager", quietly = TRUE))
install.packages("BiocManager")
BiocManager::install(c("GEOquery", "limma", "edgeR", "sva", "lme4"), dependencies = TRUE)
# Load data
exprmx <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/gene_expressions_matrix_clean.csv")
meta <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/sample_source.csv")
exprmx_mat <- as.matrix(exprmx[, -1])          # drop gene ID column
rownames(exprmx_mat) <- exprmx[[1]]            # set as rownames
meta$batch <- as.factor(meta$source)
# meta$treatment ...
# Filter low-expressed genes
perc_keep <- 0.8    # keep genes expressed at least 80% of samples
gene_keep <- rowSums(exprmx_mat > 0) >= ceiling(perc_keep * ncol(exprmx_mat))
# Create a filtered count matrix
count_tbl_low_rm <- as.matrix(exprmx_mat[gene_keep, ])
View(count_tbl_low_rm)
View(exprmx)
View(exprmx)
# Install CRAN packages
install.packages(c("data.table", "ggplot2", "ggprism"), dependencies = TRUE)
# Install Bioconductor packages
if (!require("BiocManager", quietly = TRUE))
install.packages("BiocManager")
BiocManager::install(c("GEOquery", "limma", "edgeR", "sva", "lme4"), dependencies = TRUE)
# Load data
exprmx <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/gene_expressions_matrix_clean.csv")
meta <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/sample_source.csv")
meta$batch <- as.factor(meta$source)
# meta$treatment ...
# Filter low-expressed genes
perc_keep <- 0.8    # keep genes expressed at least 80% of samples
gene_keep <- rowSums(exprmx > 0) >= ceiling(perc_keep * ncol(exprmx))
# Create a filtered count matrix
count_tbl_low_rm <- as.matrix(exprmx[gene_keep, ])
View(exprmx)
dim(exprmx)
str(exprmx[, 1:5])
gene_ids <- exprmx$HGNC
# Load data
exprmx <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/gene_expressions_matrix_clean.csv")
meta <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/sample_source.csv")
meta$batch <- as.factor(meta$source)
# meta$treatment ...
gene_ids <- exprmx$HGNC
exprmx_mat <- as.matrix(exprmx[, -1])
rownames(exprmx_mat) <- gene_ids
# Count samples where gene is expressed (> 0), ignoring NAs
n_expressed <- rowSums(exprmx_mat > 0, na.rm = TRUE)
# Count samples where gene was actually measured (not NA)
n_measured  <- rowSums(!is.na(exprmx_mat))
# Filter low-expressed genes
perc_keep <- 0.8    # keep genes expressed at least 80% of samples
# Option A — expressed in ≥80% of MEASURED samples (recommended)
# respects the fact that NAs = not measured, not absent
gene_keep <- (n_expressed / n_measured) >= perc_keep
# Create a filtered count matrix
count_tbl_low_rm <- exprmx_mat[gene_keep, ]
dim(count_tbl_low_rm)
View(count_tbl_low_rm)
print(n_expressed)
View(exprmx)
View(exprmx_mat)
View(exprmx)
View(exprmx_mat)
View(exprmx_mat)
View(exprmx)
View(exprmx_mat)
View(count_tbl_low_rm)
# Transpose count matrix for PCA
count_tbl_low_rm_t <- as.data.frame(t(count_tbl_low_rm))
# Perform PCA
pca_prep <- prcomp(count_tbl_low_rm_t, scale. = TRUE)
sum(is.na(count_tbl_low_rm))
sum(is.infinite(count_tbl_low_rm))
cat("NA rate:", round(100 * sum(is.na(count_tbl_low_rm)) / length(count_tbl_low_rm), 1), "%\n")
# How many genes have >50% samples as NA (risky to impute)
gene_na_rate <- rowMeans(is.na(count_tbl_low_rm))
table(cut(gene_na_rate, breaks = c(0, 0.1, 0.25, 0.5, 0.75, 1)))
# Load data
exprmx <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/gene_expressions_matrix_clean.csv")
meta <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/sample_source.csv")
meta$batch <- as.factor(meta$source)
# meta$treatment ...
gene_ids <- exprmx$HGNC
exprmx_mat <- as.matrix(exprmx[, -1])
rownames(exprmx_mat) <- gene_ids
# Count samples where gene is expressed (> 0), ignoring NAs
n_expressed <- rowSums(exprmx_mat > 0, na.rm = TRUE)
# Count samples where gene was actually measured (not NA)
n_measured  <- rowSums(!is.na(exprmx_mat))
# Filter low-expressed genes
perc_keep <- 0.8    # keep genes expressed at least 80% of samples
# Option A — expressed in ≥80% of MEASURED samples (recommended)
# respects the fact that NAs = not measured, not absent
gene_keep <- (n_expressed / n_measured) >= perc_keep
# Create a filtered count matrix
count_tbl_low_rm <- exprmx_mat[gene_keep, ]
dim(count_tbl_low_rm)
# Filter genes with >25% NA
gene_na_rate <- rowMeans(is.na(count_tbl_low_rm))
count_tbl_low_rm2 <- count_tbl_low_rm[gene_na_rate <= 0.25, ]
cat("Genes remaining:", nrow(count_tbl_low_rm2), "\n")  # expect ~15,570
# Row mean imputation for remaining NAs
count_tbl_pca <- t(apply(count_tbl_low_rm2, 1, function(x) {
x[is.na(x)] <- mean(x, na.rm = TRUE)
x
}))
cat("NAs after imputation:", sum(is.na(count_tbl_pca)), "\n")  # should be 0
# Transpose count matrix for PCA
count_tbl_low_rm_t <- as.data.frame(t(count_tbl_pca))
# Perform PCA
pca_prep <- prcomp(count_tbl_low_rm_t, scale. = TRUE)
# Create PCA plot colored by batch
pca_plot_batch <- autoplot(pca_prep, label = F, data = meta,
colour = "batch") +
theme_prism(base_size = 16) +
ggtitle("PCA Plot Before Batch Effect Correction") +
theme(plot.title = element_text(hjust = 0.5))
install.packages("ggfortify")
library(ggfortify)
# Perform PCA
pca_prep <- prcomp(count_tbl_low_rm_t, scale. = TRUE)
# Create PCA plot colored by batch
pca_plot_batch <- autoplot(pca_prep, label = F, data = meta,
colour = "batch") +
theme_prism(base_size = 16) +
ggtitle("PCA Plot Before Batch Effect Correction") +
theme(plot.title = element_text(hjust = 0.5))
# Load the libraries
library(data.table)
library(ggplot2)
library(ggprism)
library(limma)
library(edgeR)
library(sva)
library(lme4)
library(readr)
library(ggfortify)
library(ggprism)
# Load data
exprmx <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/gene_expressions_matrix_clean.csv")
meta <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/sample_source.csv")
meta$batch <- as.factor(meta$source)
# meta$treatment ...
gene_ids <- exprmx$HGNC
exprmx_mat <- as.matrix(exprmx[, -1])
rownames(exprmx_mat) <- gene_ids
# Count samples where gene is expressed (> 0), ignoring NAs
n_expressed <- rowSums(exprmx_mat > 0, na.rm = TRUE)
# Count samples where gene was actually measured (not NA)
n_measured  <- rowSums(!is.na(exprmx_mat))
# Filter low-expressed genes
perc_keep <- 0.8    # keep genes expressed at least 80% of samples
# Option A — expressed in ≥80% of MEASURED samples (recommended)
# respects the fact that NAs = not measured, not absent
gene_keep <- (n_expressed / n_measured) >= perc_keep
# Create a filtered count matrix
count_tbl_low_rm <- exprmx_mat[gene_keep, ]
dim(count_tbl_low_rm)
# Filter genes with >25% NA
gene_na_rate <- rowMeans(is.na(count_tbl_low_rm))
count_tbl_low_rm2 <- count_tbl_low_rm[gene_na_rate <= 0.25, ]
cat("Genes remaining:", nrow(count_tbl_low_rm2), "\n")  # expect ~15,570
# Row mean imputation for remaining NAs
count_tbl_pca <- t(apply(count_tbl_low_rm2, 1, function(x) {
x[is.na(x)] <- mean(x, na.rm = TRUE)
x
}))
cat("NAs after imputation:", sum(is.na(count_tbl_pca)), "\n")  # should be 0
# Transpose count matrix for PCA
count_tbl_low_rm_t <- as.data.frame(t(count_tbl_pca))
# Perform PCA
pca_prep <- prcomp(count_tbl_low_rm_t, scale. = TRUE)
# Create PCA plot colored by batch
pca_plot_batch <- autoplot(pca_prep, label = F, data = meta,
colour = "batch") +
theme_prism(base_size = 16) +
ggtitle("PCA Plot Before Batch Effect Correction") +
theme(plot.title = element_text(hjust = 0.5))
# Save the plot
ggsave("pca_plot_batch_unadjusted.png", pca_plot_batch,
device = "png", units = "cm", height = 12, width = 14)
# Perform PCA
pca_prep <- prcomp(count_tbl_low_rm_t, scale. = TRUE)
# Create PCA plot colored by batch
pca_plot_batch <- autoplot(pca_prep, label = F, data = meta,
colour = "batch") +
theme_prism(base_size = 16) +
ggtitle("PCA Plot Before Batch Effect Correction") +
theme(plot.title = element_text(hjust = 0.5))
# Save the plot
ggsave("pca_plot_batch_unadjusted.png", pca_plot_batch,
device = "png", units = "cm", height = 12, width = 20)
# Perform PCA
pca_prep <- prcomp(count_tbl_low_rm_t, scale. = TRUE)
# Create PCA plot colored by batch
pca_plot_batch <- autoplot(pca_prep, label = F, data = meta,
colour = "batch") +
theme_prism(base_size = 16) +
ggtitle("PCA Plot Before Batch Effect Correction") +
theme(plot.title = element_text(hjust = 0.5))
# Save the plot
ggsave("pca_plot_batch_unadjusted.png", pca_plot_batch,
device = "png", units = "cm", height = 12, width = 22)
# Plot NA rate distribution across genes
library(ggplot2)
gene_na_rate <- rowMeans(is.na(count_tbl_low_rm))
na_df <- data.frame(
bucket = c("0–10%", "10–25%", "25–50%", "50–75%", "75–100%"),
count  = as.numeric(table(cut(gene_na_rate,
breaks = c(0, 0.1, 0.25, 0.5, 0.75, 1),
include.lowest = TRUE))),
status = c("keep", "keep", "borderline", "drop", "drop")
)
na_df$bucket <- factor(na_df$bucket,
levels = c("0–10%", "10–25%", "25–50%", "50–75%", "75–100%"))
ggplot(na_df, aes(x = bucket, y = count, fill = status)) +
geom_col(width = 0.65) +
geom_text(aes(label = scales::comma(count)),
vjust = -0.5, size = 4) +
scale_fill_manual(values = c("keep"       = "#1D9E75",
"borderline" = "#EF9F27",
"drop"       = "#E24B4A"),
labels = c("keep"       = "Keep",
"borderline" = "Borderline",
"drop"       = "Drop")) +
scale_y_continuous(labels = scales::comma,
expand = expansion(mult = c(0, 0.12))) +
labs(title = "Gene missingness distribution",
x     = "NA rate per gene",
y     = "Number of genes",
fill  = NULL) +
theme_bw(base_size = 14) +
theme(plot.title   = element_text(hjust = 0.5),
legend.position = "top")
ggsave("gene_na_rate_distribution.png",
device = "png", units = "cm", height = 12, width = 14)
ggsave("plots/gene_na_rate_distribution.png",
device = "png", units = "cm", height = 12, width = 14)
# Save the plot
ggsave("plots/pca_plot_batch_unadjusted.png", pca_plot_batch,
device = "png", units = "cm", height = 12, width = 22)
View(count_tbl_low_rm_t)
# Should be TRUE for raw counts
all(count_tbl_low_rm == floor(count_tbl_low_rm), na.rm = TRUE)
# Adjust for batch effects using ComBat-seq
# Note: This method works directly on count data
count_tbl_low_rm_combat_corrected <-
ComBat_seq(count_tbl_low_rm,
batch = meta$batch,
group = meta$treatment)
# Create a DGEList object
dge <- DGEList(counts = count_tbl_low_rm, samples = meta)
batch_corrected_limma <- removeBatchEffect(count_tbl_pca,
batch = meta$batch)
# Visualize the results with PCA
pca_prep_batch_limma <- prcomp(t(batch_corrected_limma), scale. = TRUE)
limma_pca_plot <- autoplot(pca_prep_batch_limma,
label  = F,
data   = dge_v$targets,
colour = "batch") +
theme_prism(base_size = 16) +
ggtitle("PCA Plot After limma Batch Correction") +
theme(plot.title = element_text(hjust = 0.5))
batch_corrected_limma <- removeBatchEffect(count_tbl_pca,
batch = meta$batch)
# Visualize the results with PCA
pca_prep_batch_limma <- prcomp(t(batch_corrected_limma), scale. = TRUE)
limma_pca_plot <- autoplot(pca_prep_batch_limma,
label  = F,
data   = meta,
colour = "batch") +
theme_prism(base_size = 16) +
ggtitle("PCA Plot After limma Batch Correction") +
theme(plot.title = element_text(hjust = 0.5))
# Display and save the plot
print(limma_pca_plot)
ggsave("plots/pca_plot_batch_limma_corrected.png", limma_pca_plot,
device = "png", units = "cm", height = 12, width = 14)
print(limma_pca_plot)
ggsave("plots/pca_plot_batch_limma_corrected.png", limma_pca_plot,
device = "png", units = "cm", height = 12, width = 22)
# Column names of matrix vs sample IDs in meta
head(colnames(count_tbl_pca))
head(meta$sample)  # or whatever your sample ID column is called
# Are they identical and in the same order?
all(colnames(count_tbl_pca) == meta$sample)
# Column names of matrix vs sample IDs in meta
head(colnames(count_tbl_pca))
head(meta$sample)  # or whatever your sample ID column is called
# Are they identical and in the same order?
all(colnames(count_tbl_pca) == meta$batch)
View(meta)
meta
# Column names of matrix vs sample IDs in meta
head(colnames(count_tbl_pca))
head(meta$sample_id)  # or whatever your sample ID column is called
# Are they identical and in the same order?
all(colnames(count_tbl_pca) == meta$sample_id)
range(count_tbl_pca)
summary(as.vector(count_tbl_pca))
# Are most values small with a few huge outliers?
quantile(as.vector(count_tbl_pca), probs = c(0.9, 0.95, 0.99, 0.999))
meta$batch <- as.factor(meta$source)
# meta$treatment ...
gene_ids <- exprmx$HGNC
exprmx_mat <- as.matrix(exprmx[, -1])
rownames(exprmx_mat) <- gene_ids
# Count samples where gene is expressed (> 0), ignoring NAs
n_expressed <- rowSums(exprmx_mat > 0, na.rm = TRUE)
# Count samples where gene was actually measured (not NA)
n_measured  <- rowSums(!is.na(exprmx_mat))
# Filter low-expressed genes
perc_keep <- 0.8    # keep genes expressed at least 80% of samples
# Option A — expressed in ≥80% of MEASURED samples (recommended)
# respects the fact that NAs = not measured, not absent
gene_keep <- (n_expressed / n_measured) >= perc_keep
# Create a filtered count matrix
count_tbl_low_rm <- exprmx_mat[gene_keep, ]
dim(count_tbl_low_rm)
# Filter genes with >25% NA
gene_na_rate <- rowMeans(is.na(count_tbl_low_rm))
count_tbl_low_rm2 <- count_tbl_low_rm[gene_na_rate <= 0.25, ]
cat("Genes remaining:", nrow(count_tbl_low_rm2), "\n")  # expect ~15,570
# Row mean imputation for remaining NAs
count_tbl_pca <- t(apply(count_tbl_low_rm2, 1, function(x) {
x[is.na(x)] <- mean(x, na.rm = TRUE)
x
}))
cat("NAs after imputation:", sum(is.na(count_tbl_pca)), "\n")  # should be 0
# Log transform=
shift <- abs(min(count_tbl_pca)) + 1
count_tbl_log <- log2(count_tbl_pca + shift)
# Transpose count matrix for PCA
count_tbl_low_rm_t <- as.data.frame(t(count_tbl_log))
# Perform PCA
pca_prep <- prcomp(count_tbl_low_rm_t, scale. = TRUE)
# Create PCA plot colored by batch
pca_plot_batch <- autoplot(pca_prep, label = F, data = meta,
colour = "batch") +
theme_prism(base_size = 16) +
ggtitle("PCA Plot Before Batch Effect Correction") +
theme(plot.title = element_text(hjust = 0.5))
# Save the plot
ggsave("plots/pca_plot_batch_unadjusted.png", pca_plot_batch,
device = "png", units = "cm", height = 12, width = 22)
batch_corrected_limma <- removeBatchEffect(count_tbl_log,
batch = meta$batch)
# Visualize the results with PCA
pca_prep_batch_limma <- prcomp(t(batch_corrected_limma), scale. = TRUE)
limma_pca_plot <- autoplot(pca_prep_batch_limma,
label  = F,
data   = meta,
colour = "batch") +
theme_prism(base_size = 16) +
ggtitle("PCA Plot After limma Batch Correction") +
theme(plot.title = element_text(hjust = 0.5))
# Display and save the plot
print(limma_pca_plot)
ggsave("plots/pca_plot_batch_limma_corrected.png", limma_pca_plot,
device = "png", units = "cm", height = 12, width = 22)
# How many samples per batch?
table(meta$batch)
# Check if green outliers are genuine biological outliers or technical
# Look at which green samples are far from the origin
pca_df <- as.data.frame(pca_after$x)
# How many samples per batch?
table(meta$batch)
# Check if green outliers are genuine biological outliers or technical
# Look at which green samples are far from the origin
pca_df <- as.data.frame(limma_pca_plot$x)
pca_df$batch <- meta$batch
# How many samples per batch?
table(meta$batch)
# Check if green outliers are genuine biological outliers or technical
# Look at which green samples are far from the origin
pca_df <- as.data.frame(limma_pca_plot$x)
pca_df$batch <- meta$batch
View(meta)
pca_df$batch <- meta$batch
View(meta)
View(batch_corrected_limma)
# Convert to dataframe with gene names as first column
batch_corrected_df <- as.data.frame(batch_corrected_limma)
batch_corrected_df <- cbind(HGNC = rownames(batch_corrected_limma), batch_corrected_df)
# Save to csv
write_csv(batch_corrected_df,
"~/PycharmProjects/internship-m2-melanoma/main/dataset/gene_expressions_matrix_corrected.csv")
# Save to csv
write_csv(batch_corrected_df,
"~/PycharmProjects/internship-m2-melanoma/main/dataset/gene_expressions_matrix_corrected.csv")
# Load the libraries
library(data.table)
library(ggplot2)
library(ggprism)
library(limma)
library(edgeR)
library(sva)
library(lme4)
library(readr)
library(ggfortify)
library(ggprism)
# Load data
exprmx <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/created/gex_mat_rna_seq.csv")
meta <- read_csv("~/PycharmProjects/internship-m2-melanoma/main/dataset/created/sample_source_rna_seq.csv")
meta$batch <- as.factor(meta$source)
# meta$treatment ...
gene_ids <- exprmx$HGNC
exprmx_mat <- as.matrix(exprmx[, -1])
rownames(exprmx_mat) <- gene_ids
# Count samples where gene is expressed (> 0), ignoring NAs
n_expressed <- rowSums(exprmx_mat > 0, na.rm = TRUE)
# Count samples where gene was actually measured (not NA)
n_measured  <- rowSums(!is.na(exprmx_mat))
# Filter low-expressed genes
perc_keep <- 0.8    # keep genes expressed at least 80% of samples
# Option A — expressed in ≥80% of MEASURED samples (recommended)
# respects the fact that NAs = not measured, not absent
gene_keep <- (n_expressed / n_measured) >= perc_keep
# Create a filtered count matrix
count_tbl_low_rm <- exprmx_mat[gene_keep, ]
dim(count_tbl_low_rm)
# Filter genes with >25% NA
gene_na_rate <- rowMeans(is.na(count_tbl_low_rm))
count_tbl_low_rm2 <- count_tbl_low_rm[gene_na_rate <= 0.25, ]
cat("Genes remaining:", nrow(count_tbl_low_rm2), "\n")  # expect ~15,570
# Row mean imputation for remaining NAs
count_tbl_pca <- t(apply(count_tbl_low_rm2, 1, function(x) {
x[is.na(x)] <- mean(x, na.rm = TRUE)
x
}))
cat("NAs after imputation:", sum(is.na(count_tbl_pca)), "\n")  # should be 0
# Log transform=
shift <- abs(min(count_tbl_pca)) + 1
count_tbl_log <- log2(count_tbl_pca + shift)
# Transpose count matrix for PCA
count_tbl_low_rm_t <- as.data.frame(t(count_tbl_log))
# Perform PCA
pca_prep <- prcomp(count_tbl_low_rm_t, scale. = TRUE)
# Create PCA plot colored by batch
pca_plot_batch <- autoplot(pca_prep, label = F, data = meta,
colour = "batch") +
theme_prism(base_size = 16) +
ggtitle("PCA Plot Before Batch Effect Correction") +
theme(plot.title = element_text(hjust = 0.5))
# Save the plot
ggsave("plots/pca_plot_batch_unadjusted.png", pca_plot_batch,
device = "png", units = "cm", height = 12, width = 22)
batch_corrected_limma <- removeBatchEffect(count_tbl_log,
batch = meta$batch)
# Visualize the results with PCA
pca_prep_batch_limma <- prcomp(t(batch_corrected_limma), scale. = TRUE)
limma_pca_plot <- autoplot(pca_prep_batch_limma,
label  = F,
data   = meta,
colour = "batch") +
theme_prism(base_size = 16) +
ggtitle("PCA Plot After limma Batch Correction") +
theme(plot.title = element_text(hjust = 0.5))
# Display and save the plot
print(limma_pca_plot)
ggsave("plots/pca_plot_batch_limma_corrected.png", limma_pca_plot,
device = "png", units = "cm", height = 12, width = 22)
# Convert to dataframe with gene names as first column
batch_corrected_df <- as.data.frame(batch_corrected_limma)
batch_corrected_df <- cbind(HGNC = rownames(batch_corrected_limma), batch_corrected_df)
# Save to csv
write_csv(batch_corrected_df,
"~/PycharmProjects/internship-m2-melanoma/main/dataset/gene_expressions_matrix_corrected.csv")
